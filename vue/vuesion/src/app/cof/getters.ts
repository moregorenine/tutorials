import { ICofState } from './state';

export interface ICofGetters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<ICofState, IState>, param: any): Promise<any>;
   */
}

export const CofGetters: ICofGetters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: ICofState) {
   *   return state.myAttribute;
   * }
   */
};
